<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.zzz8688.diagagent.mapper.UserMapper">

    <resultMap id="UserResultMap" type="io.github.zzz8688.diagagent.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="salt" column="salt"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="status" column="status"/>
        <result property="role" column="role"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdTime" column="created_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password, salt, nickname, avatar, status, role, last_login_time, created_time
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <update id="updateLastLoginTime">
        UPDATE user
        SET last_login_time = #{lastLoginTime}
        WHERE id = #{id}
    </update>
</mapper>
